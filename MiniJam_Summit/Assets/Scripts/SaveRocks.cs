using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveRocks : MonoBehaviour
{
    //List<Vector3> rocks;
    //public GameObject rock_prefab;

    // Start is called before the first frame update
    void Start()
    {
        //rocks = new List<Vector3>();
        //foreach(Transform c in transform)
        //{
        //    rocks.Add(c.position);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void Restart()
    //{
    //    foreach(Transform i in transform)
    //    {
    //        Destroy(i.gameObject);
    //    }

    //    foreach (Vector3 i in rocks)
    //    {
    //        var o = Instantiate<GameObject>(rock_prefab);
    //        o.transform.position = i;
    //        o.transform.rotation = Quaternion.Euler(90f, 0f, 0f);
    //        o.transform.localScale = new Vector3(0.136f, 0.136f, 0.136f);
    //        o.tag = "Obstacle";
    //    }
    //}
}
